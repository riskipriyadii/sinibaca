---
const base = import.meta.env.BASE_URL.replace(/\/$/, '') + '/';
---

<div class="newsletter-card">
  <h4 class="newsletter-title">Ikut Membaca</h4>
  <p class="newsletter-subtitle">Rilis kalau sudah matang.</p>
  
  <p class="newsletter-text">Tulisan baru akan diumumkan lewat Instagram.</p>
  
  <a href={`${base}blog`} class="newsletter-link">
    <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z"></path><circle cx="12" cy="12" r="3"></circle></svg>
    Dikirim kalau memang layak.
  </a>
</div>

<style>
  .newsletter-card {
    background: linear-gradient(135deg, #1a1a2e 0%, #16213e 100%);
    border: 1px solid rgba(99, 102, 241, 0.2);
    border-radius: 12px;
    padding: 1.25rem;
    color: #fff;
  }
  
  .newsletter-title {
    font-size: 0.9rem;
    font-weight: 700;
    margin: 0 0 0.25rem;
    color: #fff;
  }
  
  .newsletter-subtitle {
    font-size: 0.75rem;
    color: #94a3b8;
    margin: 0 0 0.75rem;
  }
  
  .newsletter-form {
    display: flex;
    gap: 0.5rem;
    margin-bottom: 0.75rem;
  }
  
  .newsletter-input {
    flex: 1;
    min-width: 0;
    padding: 0.5rem 0.75rem;
    border: 1px solid rgba(255,255,255,0.1);
    border-radius: 6px;
    background: rgba(255,255,255,0.05);
    color: #fff;
    font-size: 0.8rem;
    font-family: inherit;
    outline: none;
    transition: border-color 0.2s;
  }
  
  .newsletter-input::placeholder {
    color: #64748b;
  }
  
  .newsletter-input:focus {
    border-color: var(--accent, #6366f1);
  }
  
  .newsletter-btn {
    padding: 0.5rem 0.75rem;
    background: var(--accent, #6366f1);
    color: #fff;
    border: none;
    border-radius: 6px;
    font-size: 0.75rem;
    font-weight: 600;
    cursor: pointer;
    transition: background 0.2s, transform 0.1s;
    white-space: nowrap;
    flex-shrink: 0;
  }
  
  .newsletter-btn:hover {
    background: #4f46e5;
    transform: translateY(-1px);
  }
  
  .newsletter-btn:active {
    transform: translateY(0);
  }
  
  .newsletter-link {
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    font-size: 0.75rem;
    color: #94a3b8;
    text-decoration: none;
    transition: color 0.2s;
  }
  
  .newsletter-link:hover {
    color: #fff;
  }
  
  .newsletter-link svg {
    flex-shrink: 0;
  }
</style>